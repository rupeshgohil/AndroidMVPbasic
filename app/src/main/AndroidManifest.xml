<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aru.androidmvpbasic">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeDemo">
        <activity android:name=".moviedetails.MovieDetailsActivity"
            android:theme="@style/NoActionBar"/>
        <activity
            android:name=".moviefilter.MovieFilterActivity"
            android:label="@string/title_activity_movie_filter"
            android:theme="@style/AppThemeDemo.NoActionBar" />
        <activity
            android:name=".movielist.MainActivity"
            android:label="@string/title_activity_splacescreen"
            android:theme="@style/NoActionBar" />
        <activity android:name=".SplacescreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>